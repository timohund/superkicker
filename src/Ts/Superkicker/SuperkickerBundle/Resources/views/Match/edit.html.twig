<!DOCTYPE html>
<html>
<head lang="en">
	<meta charset="UTF-8">
	<title>Match anlegen</title>
</head>
<body>

<h1>Match anlegen</h1>

<form action="{{ path('ts_superkicker_match_save') }}" id="matchCreate" method="post">
	<input type="hidden" name="matchId" value="{{ matchId }}"/>
	<p>Heim</p>
	<select name="match[home]">
		{% for club in allClubs %}
			<option value="{{ club.id }}" >{{ club.name }}</option>
		{% endfor %}
	</select>

	<p>Gast</p>
	<select name="match[guest]">
		{% for club in allClubs %}
			<option value="{{ club.id }}" >{{ club.name }}</option>
		{% endfor %}
	</select>

	<p>Spieltag</p>
	<select name="match[day]">
		{%for day in 1..34%}
			<option value="{{ day }}">{{ day }}</option>
		{% endfor %}
	</select>
	<input type="submit" />
</form>
</body>

<a href="{{ path('ts_superkicker_index') }}">Start</a>
</html>